{
  "name": "@nteract/preview-python",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @nteract/pyodide-runtime build && node scripts/build-provider.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": { "@nteract/pyodide-runtime": "workspace:*" },
  "devDependencies": { "esbuild": "^0.28.0" }
}
