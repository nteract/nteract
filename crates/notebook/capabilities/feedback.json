{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "feedback",
  "description": "Minimal capabilities for the feedback window",
  "windows": ["feedback"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-close",
    "core:window:allow-set-theme",
    "shell:default"
  ]
}
